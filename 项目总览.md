# 倒数日 CountdownApp — 项目总览

> 更新：2026-07-11

---

## 🎯 功能矩阵

| 模块 | 功能 | 说明 | 状态 |
|------|------|------|:----:|
| 📌 事件管理 | 新建事件 | 输入名称 + 选日期（阳历/农历），一键添加 | ✅ |
| | 事件列表 | 卡片式瀑布列表，按置顶+日期排序 | ✅ |
| | 编辑/修改 | 长按进入编辑模式，多选/全选/批量删除 | ✅ |
| | 滑动删除 | 左滑露出红色删除按钮 | ✅ |
| | 滑动置顶 | 右滑露出天蓝置顶按钮，上限 5 个 | ✅ |
| 📅 农历支持 | 阳历/农历切换 | 添加事件时 Tab 切换，滚轮选日期 | ✅ |
| | 农历生日重复 | 勾选「每年重复」，自动按农历生日逐年计算 | ✅ |
| | 中文日期显示 | 正月、腊月、初一、十五 等中文格式 | ✅ |
| 🖼️ 卡片个性化 | 自定义背景图 | 从相册选取图片设为卡片背景 | ✅ |
| | 自动调色 | 根据图片亮度自动切换黑/白文字色 | ✅ |
| | 导出卡片图片 | 详情页一键保存卡片为 PNG 到相册 | ✅ |
| | 动态主题色 | 事件名称哈希生成专属左侧色条 | ✅ |
| 🎨 交互 | 毛玻璃动效 | 进入详情页时背景模糊渐变 | ✅ |
| | 滑动互斥 | 多个滑动卡片互斥，滚动列表自动关闭 | ✅ |

---

## 🏛️ 整体架构

```
┌─────────────────────────────────────────────────────────────┐
│                    UI 层（Jetpack Compose）                    │
│                                                             │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────────┐   │
│  │   MainScreen  │  │EventDetail   │  │  AddEventDialog  │   │
│  │  (事件列表)   │  │Screen(详情)  │  │  (添加弹窗)      │   │
│  └──────┬───────┘  └──────┬───────┘  └──────────────────┘   │
│         │                  │                                 │
│  ┌──────┴──────────────────┴───────┐                        │
│  │        UI Components            │                        │
│  │  ┌──────────┐ ┌──────────────┐  │                        │
│  │  │EventCard │ │SwipeToDelete │  │                        │
│  │  │(卡片渲染)│ │(左右滑交互)  │  │                        │
│  │  └──────────┘ └──────────────┘  │                        │
│  └─────────────────────────────────┘                        │
│                                                             │
│  ┌─────────────────────────────────────────────────┐        │
│  │             MainViewModel (StateFlow)           │        │
│  └─────────────────────┬───────────────────────────┘        │
└────────────────────────┼────────────────────────────────────┘
                         │
┌────────────────────────┼────────────────────────────────────┐
│              数据层 (Repository + Room)                     │
│                                                             │
│  ┌──────────────────────────────────────────────────┐       │
│  │            EventRepository                       │       │
│  │  CRUD / 批量删除 / 置顶 / 更新背景 / 更新日期    │       │
│  └──────────┬───────────────────────────────────────┘       │
│             │                                               │
│  ┌──────────┴──────────┐                                    │
│  │    EventDao (Room)  │                                    │
│  │  @Query / @Insert   │                                    │
│  │  @Delete / @Update  │                                    │
│  └──────────┬──────────┘                                    │
│             │                                               │
│  ┌──────────┴──────────┐                                    │
│  │  AppDatabase (Room)  │                                    │
│  │  countdown_database  │                                    │
│  └─────────────────────┘                                    │
└─────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────┐
│                    工具层 (Utility)                          │
│                                                             │
│  ┌──────────────┐  ┌────────────────────┐                   │
│  │  DateUtils   │  │  LunarDateUtils    │                   │
│  │  · 天数计算  │  │  · 阳历↔农历转换   │                   │
│  │  · 颜色生成  │  │  · 中文日期格式化  │                   │
│  │  · 日常判断  │  │  · 农历重复计算    │                   │
│  └──────────────┘  └────────────────────┘                   │
└─────────────────────────────────────────────────────────────┘
```

---

## 📐 技术栈

| 层次 | 技术 | 版本 | 用途 |
|:-----|:-----|:-----|:-----|
| **语言** | Kotlin | 1.9.22 | 全部代码 |
| **UI 框架** | Jetpack Compose | BOM 2024.02.00 | 声明式 UI |
| | Material 3 | — | Material Design 组件 |
| | Compose Animation | — | 毛玻璃动效、卡片展开 |
| | Navigation Compose | 2.7.7 | 页面路由（单 Activity） |
| **状态管理** | ViewModel + StateFlow | 2.7.0 | MVVM 数据流 |
| **数据库** | Room | 2.6.1 | 本地持久化 |
| | KSP | 1.9.22-1.0.17 | Room 编译期代码生成 |
| **图片** | Coil | 2.5.0 | 异步加载本地背景图 |
| **农历** | cn.6tail:lunar | 1.7.4 | 阳历/农历双向转换 |
| **构建** | Gradle | 8.5 | 构建系统 |
| | Android Gradle Plugin | 8.2.2 | Android 构建 |
| **编译** | JDK 17 | — | Java 编译 |
| **SDK** | compileSdk 34 / minSdk 26 | — | Android 版本兼容 |

---

## 🗄️ 数据模型

### Event 实体

| 字段 | 类型 | 说明 |
|:-----|:-----|:-----|
| `id` | Long (PK, autoGenerate) | 主键 |
| `name` | String | 事件名称 |
| `targetDate` | Long (millis) | 目标日期时间戳 |
| `backgroundImagePath` | String? | 自定义背景图路径 |
| `createdAt` | Long (millis) | 创建时间 |
| `recurringMonth` | Int (-1 ~ 11) | 每年重复的月份，-1 表示不重复 |
| `recurringDay` | Int (-1 ~ 31) | 每年重复的日 |
| `isPinned` | Boolean | 是否置顶 |
| `pinnedTime` | Long | 置顶时间（排序用） |
| `calendarType` | String ("solar" / "lunar") | 日历类型 |
| `lunarYear` | Int | 农历年份 |
| `lunarMonth` | Int | 农历月份 |
| `lunarDay` | Int | 农历日 |
| `isLeapMonth` | Boolean | 是否闰月 |
| `isRepeatYearly` | Boolean | 农历是否每年重复 |

### 排序规则

```
ORDER BY isPinned DESC, pinnedTime DESC, targetDate ASC
```

置顶事件排最前 → 按置顶时间倒序 → 按目标日期正序

---

## 🔄 核心数据流

### 天数计算逻辑

```
                ┌─────────────────────────┐
                │    calculateEventDays() │
                └──────────┬──────────────┘
                           │
            ┌──────────────┼──────────────┐
            ▼              ▼              ▼
    ┌─────────────┐ ┌──────────┐ ┌────────────────┐
    │  公历普通   │ │ 公历重复 │ │   农历重复     │
    │ targetDate  │ │recurring │ │ lunarMonth/    │
    │ - today     │ │Month/Day │ │ lunarDay       │
    │             │ │ 找最近   │ │ 用lunar库      │
    │             │ │ 一次日期 │ │ 逐年查最近一次 │
    └─────────────┘ └──────────┘ └────────────────┘
```

### 交互流程

```
┌──────────┐    ┌──────────────┐    ┌────────────────┐
│  首页    │───→│ EventDetail  │───→│  保存卡片图片  │
│ 列表卡片 │    │ 大卡片详情页 │    │  到相册        │
│          │    │ · 修改名称   │    └────────────────┘
│ 单击→    │    │ · 修改日期   │
│ 详情页   │    │ · 设置背景   │
│          │    │ · 导出图片   │
│ 左滑→删  │    │ · 置顶/取消  │
│ 右滑→置顶│    └──────────────┘
│ 长按→编辑│
└──────────┘
```

---

## 🧩 UI 组件树

```
CountdownAppContent (MainActivity)
├── MainScreen
│   ├── TopAppBar (正常模式 / 编辑模式)
│   ├── LazyColumn
│   │   └── SwipeToDeleteContainer
│   │       ├── 删除按钮 (左滑露出，红色)
│   │       ├── 置顶按钮 (右滑露出，天蓝)
│   │       └── EventCard
│   │           ├── 普通卡片 (80dp)
│   │           └── 置顶大卡片 (160dp, 📌 图标)
│   ├── FAB (+) → AddEventDialog
│   │   ├── 名称输入框
│   │   ├── 阳历 / 农历 Tab
│   │   ├── 日期滚轮选择器
│   │   └── 农历 → "每年重复"复选框
│   └── BottomBar (编辑模式：删除按钮)
│
├── 毛玻璃遮罩层 (动画：0 → 0.6 alpha)
│
└── EventDetailScreen (覆盖层)
    ├── 背景图 (Coil) / 纯色
    ├── 事件名称
    ├── 大字倒计时 + 天
    ├── 目标日期
    ├── 导出图片按钮 (右上角 ⬇)
    ├── 修改名称
    ├── 修改日期
    └── 设置背景
```

---

## 🗺️ 页面路由

```
单 Activity 架构 (ComponentActivity)
┌──────────────────────────────────────┐
│  setContent → CountdownAppTheme      │
│    └── CountdownAppContent           │
│         ├── MainScreen (always)      │
│         └── EventDetailScreen        │
│             (条件渲染: selectedEvent) │
└──────────────────────────────────────┘
```

无 Navigation Compose 路由跳转，详情页以覆盖层（overlay）形式直接叠加在首页之上，通过 `selectedEvent` 状态控制显示/隐藏，配合 `animateFloatAsState` 实现毛玻璃渐入动效。

---

## 🔌 关键依赖

```kotlin
// 核心
androidx.core:core-ktx:1.12.0
androidx.lifecycle:lifecycle-runtime-ktx:2.7.0
androidx.activity:activity-compose:1.8.2

// Compose
androidx.compose:compose-bom:2024.02.00
androidx.compose.ui:ui
androidx.compose.material3:material3
androidx.compose.material:material-icons-extended

// 架构
androidx.lifecycle:lifecycle-viewmodel-compose:2.7.0
androidx.navigation:navigation-compose:2.7.7

// 数据库
androidx.room:room-runtime:2.6.1
androidx.room:room-ktx:2.6.1
com.google.devtools.ksp → room-compiler:2.6.1

// 图片
io.coil-kt:coil-compose:2.5.0

// 农历
cn.6tail:lunar:1.7.4
```

---

## 🐛 已知问题

| 问题 | 影响 | 说明 |
|:-----|:-----|:-----|
| 农历日期首页显示不完整 | 中等 | 农历日期在 EventCard 上的格式化显示尚未完全集成 |
| 置顶大卡片样式细节 | 低 | 置顶卡片样式细节可进一步优化（更多信息展示） |
| 深色模式 | 未来 | 目前仅支持浅色主题 |
| 事件分类/标签 | 未来 | 暂无分类管理功能 |

---

## 📁 项目结构速览

```
CountdownApp/
├── app/
│   ├── build.gradle.kts          # 模块构建（依赖 + Compose 配置）
│   └── src/main/
│       ├── AndroidManifest.xml
│       ├── java/com/countdownapp/
│       │   ├── CountdownApp.kt          # Application
│       │   ├── MainActivity.kt          # 主 Activity + UI 组合
│       │   ├── data/
│       │   │   ├── entity/Event.kt      # 数据模型
│       │   │   ├── db/AppDatabase.kt    # 数据库
│       │   │   ├── db/EventDao.kt       # DAO
│       │   │   └── repository/          # 仓库
│       │   ├── ui/
│       │   │   ├── theme/               # 主题/颜色
│       │   │   ├── components/          # 可复用组件
│       │   │   ├── screens/             # 页面
│       │   │   └── viewmodel/           # ViewModel
│       │   └── util/                    # 工具类
│       └── res/                         # 资源
├── build.gradle.kts                     # 根构建
├── settings.gradle.kts                  # Gradle 设置
├── gradle.properties                    # Gradle 属性
├── gradlew.bat                          # Wrapper
├── .gitignore
├── PROGRESS.md                          # 开发进度
├── USER_GUIDE.md                        # 使用指南
├── PROJECT_BOOK.md                      # 项目说明书
└── README.md                            # GitHub 首页
```
