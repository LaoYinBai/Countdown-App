# 发布签名配置模板。复制为 keystore.properties 并填入真实值。
# keystore.properties 与 keystore 文件均已被 .gitignore 排除，不会入库。
# 缺失 keystore.properties 时构建照常进行，release 产物为未签名 APK。
#
# 相对路径以仓库根目录（settings.gradle.kts 所在目录）为基准。
storeFile=countdownapp-release.jks
storePassword=在此填写
keyAlias=countdownapp
keyPassword=在此填写
